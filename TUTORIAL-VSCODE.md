# 🖱️ 在 VS Code 里用 Git（点按钮版教程）

> 不爱敲命令？没问题。**VS Code 内置了完整的 Git 功能，全程点按钮就能用。**
> 这份教程和命令行版（`TUTORIAL.md`）教的是同一套东西，只是把"敲命令"换成了"点鼠标"。
> 还是拿 `todo-app` 这个真实项目来练。

---

## 怎么用这份教程

- 看到 🎯 = 这一步对应课程哪个概念，🖱️ = 该点哪里，💡 = 为什么，✋ = 小贴士。
- 按钮名我会给两种：`Initialize Repository（初始化仓库）`——前面是英文界面、括号里是中文界面，**照着找你能看到的那个**。
- 不用一口气做完，一关一关来。

---

## 🗺️ 先认识界面：Git 藏在哪？

VS Code 最左边那一竖排图标，叫**活动栏**。其中有一个是 Git 的家：

```
┌─ 活动栏（最左边一竖排）──┐
│  📄  资源管理器（文件列表） │
│  🔍  搜索                  │
│  ⑂   源代码管理 ★ Git 在这！│  ← 长得像"分叉/三个点连线"
│  ▶   运行和调试            │
│  ⬛  扩展                  │
└───────────────────────────┘
```

🖱️ 那个像"分叉"的图标，英文叫 **Source Control（源代码管理）**。点它，或者按快捷键 **`Ctrl/Cmd + Shift + G`**，就打开了 Git 面板。这是你接下来最常用的地方。

---

## 第 0 关 · 准备：装好并打开项目

🎯 *对应课程：模块 1*

### 0.1 确认装了 VS Code

如果还没装：去 [code.visualstudio.com](https://code.visualstudio.com) 下载安装。Git 它自带集成，但**电脑上得装过 Git**——你做命令行教程时如果跑通了 `git --version`，就已经有了。

### 0.2 用 VS Code 打开 todo-app 文件夹

🖱️ 顶部菜单 **File → Open Folder…（文件 → 打开文件夹…）**，选中 `~/Desktop/git-teach/todo-app`，打开。

✋ 一定是**"打开文件夹"**，不是"打开文件"。Git 是对整个文件夹工作的。打开后，左边「资源管理器」里应该能看到 `index.html`、`app.js` 等文件。

### 0.3 告诉 Git "你是谁"（第一次用必做一次）⚠️

每个存档点都要记录"是谁提交的"，所以**第一次用 Git 前，得先自报家门**——否则你点 Commit 时会弹出一个黄色警告：*"请确保已在 Git 中配置你的 user.name 和 user.email"*，提交被拦下。

🖱️ 配置方法：打开 VS Code 底部的终端（顶部菜单 **Terminal → New Terminal / 终端 → 新建终端**），把下面两行粘进去，**把名字和邮箱换成你自己的**，每行回车：

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

💡 `--global` 这个开关的意思是"这台电脑上所有项目都用这个身份"，所以**设一次就够，以后不用再设**。配好后回去重新点 Commit 就能成功了。

✋ 这个名字和邮箱会**记录在每个提交里**，将来推到 GitHub 后别人能看到。用你愿意公开的就行（一般就用 GitHub 注册的那个邮箱）。想改随时再跑一遍上面两行即可。

---

## 第 1 关 · 初始化仓库（图形版 git init）

🎯 *对应课程：模块 2——建"货舱"*

🖱️ 点左边活动栏的 **源代码管理（⑂）** 图标。因为这个文件夹还不是 Git 项目，你会看到一个蓝色按钮：

```
┌─ 源代码管理 ─────────────────────┐
│                                  │
│   ┌──────────────────────────┐   │
│   │  Initialize Repository   │   │ ← 点这个蓝色按钮
│   │      （初始化仓库）        │   │
│   └──────────────────────────┘   │
│                                  │
│   ┌──────────────────────────┐   │
│   │   Publish to GitHub      │   │ （这个先不用，第 6 关再说）
│   └──────────────────────────┘   │
└──────────────────────────────────┘
```

🖱️ 点 **Initialize Repository（初始化仓库）**。

💡 这一下等于命令行的 `git init`。VS Code 悄悄建好了 `.git`"货舱"。点完，面板立刻变样——它开始列出你所有的文件了。

---

## 第 2 关 · 创建第一个存档点（图形版 add + commit）

🎯 *对应课程：模块 3——数据从"家里"流到"货舱"*

初始化后，源代码管理面板现在长这样：

```
┌─ 源代码管理 ───────────── ✓ Commit  ⟳  ⋯ ┐
│ ┌──────────────────────────────────────┐ │
│ │  Message（在这写这次改动的说明）        │ │ ← ① 提交说明输入框
│ └──────────────────────────────────────┘ │
│                                          │
│ ▾ Changes（改动）                    6    │ ← 所有文件列在这
│     index.html                    ↩  +   │ ← hover 文件，右边出现图标
│     style.css                     ↩  +   │
│     app.js                        ↩  +   │
│     ...                                  │
└──────────────────────────────────────────┘
        ↑ 把鼠标移到文件上，右边会冒出：
          ↩ = Discard（丢弃改动）   + = Stage（送上柜台）
```

### 2.1 把文件送上"柜台"（Stage = git add）

🖱️ 有两种点法：
- **逐个**：把鼠标移到某个文件上，点右边的 **`+`**（Stage Changes / 暂存更改）。
- **全部**：把鼠标移到 **`Changes（更改）`** 这一行标题上，点右边出现的 **`+`**，一次性全送上柜台。

点完，文件会移动到一个新分组 **`Staged Changes（暂存的更改）`** 里。

💡 这个 `+` 就是命令行的 `git add`。"柜台"上的东西，才会被打包进下次存档。

✋ 想把某个文件从柜台撤下来？点它右边的 **`−`**（Unstage / 取消暂存）即可，不影响文件内容。

### 2.2 写说明 + 提交（Commit）

🖱️ ① 在最上面的 **Message（说明）** 框里，输入一句话，比如：`初始版本：能用的待办清单`
🖱️ ② 点上方那个带对勾的 **✓ Commit（提交）** 按钮（或按 `Cmd/Ctrl + Enter`）。

```
┌──────────────────────────────────────────┐
│ │ 初始版本：能用的待办清单            │      │ ← ① 写在这
│ └──────────────────────────────────┘      │
│        ┌──────────────────┐               │
│        │   ✓ Commit       │  ← ② 点这     │
│        └──────────────────┘               │
└──────────────────────────────────────────┘
```

🎉 **第一个存档点创建成功！** 面板里的文件列表清空了——因为它们都安全地进了"货舱"。

✋ 如果它弹窗问 *"是否要暂存所有改动并直接提交？"*，说明你忘了先 Stage。点"是/Yes"也行，VS Code 会帮你一步到位。

---

## 第 3 关 · 改一处代码，看 diff（图形版最爽的部分）

🎯 *对应课程：模块 5——用 diff 看穿改了什么*

### 3.1 改个东西

🖱️ 在左边「资源管理器」点开 `index.html`，找到这行：

```html
<p class="subtitle">一个用来练习 Git 的小项目</p>
```

改成：

```html
<p class="subtitle">一个用来练习 Git 的小项目 · 加油！</p>
```

按 `Cmd/Ctrl + S` 保存。

### 3.2 见证：VS Code 到处给你提示

文件一改，VS Code 会**自动**提示你：
- 左边「资源管理器」里 `index.html` 文件名变成**橙黄色**，后面带个字母 **`M`**（Modified，改过了）。
- 源代码管理图标 **⑂** 上出现一个**蓝色数字角标**，告诉你有几个文件改了。

💡 不用敲 `git status`——这些颜色和角标就是你的"状态仪表盘"，时刻告诉你哪些文件动过。

### 3.3 点开看 diff（左右对比）

🖱️ 进源代码管理面板，**点一下 `index.html` 这个文件名**。VS Code 会打开一个**左右并排的对比视图**：

```
┌─ 改之前（旧）──────────┬─ 改之后（新）─────────────────┐
│ <p>…小项目</p>         │ <p>…小项目 · 加油！</p>        │
│        ▲ 红色背景      │            ▲ 绿色背景          │
└────────────────────────┴───────────────────────────────┘
```

💡 **左边红色 = 原来的，右边绿色 = 改成的。** 这就是图形版的 `git diff`，比命令行还直观。以后 AI 改完代码，你点开每个文件这样一看，它到底动了啥一目了然，想骗你都难。

### 3.4 暂存 + 提交

🖱️ 和第 2 关一样：点 `index.html` 右边的 `+` 暂存 → 在 Message 框写 `副标题加一句鼓励语` → 点 **✓ Commit**。

---

## 第 4 关 · 用分支加新功能（图形版，⭐ 最有用）

🎯 *对应课程：模块 4——分支是平行时间线*

VS Code 把"你在哪条分支"显示在**最左下角的状态栏**：

```
┌─ VS Code 最底部状态栏 ─────────────────────┐
│  ⑂ main   ⟳ 0↑ 0↓   ...                    │
│    ▲                                        │
│    └─ 当前分支名。点它就能切换/新建分支！      │
└────────────────────────────────────────────┘
```

### 4.1 新建并切到一条分支

🖱️ **点左下角的 `⑂ main`**。顶部会弹出一个菜单，选 **`Create new branch…（创建新分支…）`**，输入分支名 `feature/clear-done`，回车。

💡 等于命令行 `git switch -c feature/clear-done`。现在左下角变成了 `⑂ feature/clear-done`——你进了平行宇宙，`main` 安然不动。

### 4.2 真实地加功能（两处改动）

**改动一**：打开 `index.html`，找到底部这行：
```html
<span id="counter" class="counter">还有 0 件事要做</span>
```
在它**下面**加一行：
```html
<button id="clear-done" class="clear-btn">清除已完成</button>
```

**改动二**：打开 `app.js`，拉到**最底部**，在最后那行 `render();` **之前**，加上：
```javascript
// "清除已完成"按钮：一次性删掉所有打了勾的任务
document.getElementById("clear-done").addEventListener("click", () => {
  tasks = tasks.filter((t) => !t.done);
  save();
  render();
});
```

保存两个文件。

✋ **现在就试！** 在资源管理器里右键 `index.html` → **Open with Live Server**（如果装了该扩展），或直接在 Finder 里双击 `index.html`。加几个任务、勾掉、点「清除已完成」——真的能用！

### 4.3 提交这个功能

🖱️ 源代码管理面板 → 把 `index.html` 和 `app.js` 都 Stage（点 `+`）→ 写说明 `加了'清除已完成'按钮` → ✓ Commit。

### 4.4 见证奇迹：切回 main，功能"消失"

🖱️ 点左下角分支名 → 选 **`main`** 切回主线。

现在看 `index.html` 和 `app.js`——你刚加的代码**不见了**！再打开网页，「清除已完成」按钮也没了。

💡 它没丢，只是只活在 `feature/clear-done` 那条平行宇宙里。`main` 这条线上从来没有过它。

### 4.5 合并回 main

🖱️ 确保你现在在 `main`（左下角显示 `⑂ main`）。然后：
点源代码管理面板右上角的 **`⋯`（更多操作）** → **`Branch（分支）` → `Merge Branch…（合并分支…）`** → 选 `feature/clear-done`。

💡 等于命令行 `git merge feature/clear-done`。代码回来了，这次是在 `main` 上。功能正式并入主线。

✋ 找不到 `⋯` 菜单？还有个万能入口：按 **`Cmd/Ctrl + Shift + P`** 打开命令面板，输入 `Git: Merge`，就能找到合并命令。**所有 Git 操作都能在这个命令面板里搜到。**

---

## 第 5 关 · 后悔药（图形版撤销）

🎯 *对应课程：模块 6——restore / 撤销*

### 还没提交的烂改动，一键丢弃

假装你把 `app.js` 乱改一通搞崩了（还没提交）。想一键还原：

🖱️ 源代码管理面板 → 把鼠标移到 `app.js` 上 → 点右边那个 **`↩`（Discard Changes / 放弃更改）**。VS Code 会确认一下，点确定，文件立刻变回上次提交的样子。

💡 这就是图形版的 `git restore`——"这几下白改了，撤回"。

✋ ⚠️ `↩` 丢弃的是**还没提交**的改动，丢了找不回。所以丢之前确认你真的不要了。已经提交过的东西不受影响（那些有存档点保底）。

### 想撤销"已经提交"的改动？

🖱️ 命令面板（`Cmd/Ctrl + Shift + P`）输入 `Git: Revert Commit`，选要撤销的那个提交。它会**补一个新提交去抵消**旧的——安全、有据可查，对应课程讲的 `revert`。

---

## 第 6 关（可选）· 推到 GitHub（图形版）

🎯 *对应课程：模块 4——远程仓库、push*

VS Code 把"发布到云端"做成了一个按钮。提交过之后，源代码管理面板顶部会出现：

🖱️ 一个蓝色的 **`Publish Branch（发布分支）`** 按钮 → 点它 → VS Code 会让你登录 GitHub（跟着浏览器弹窗授权一下）→ 选公开(public)还是私有(private) → 搞定。

💡 它一步帮你做完了命令行要敲的好几条（建远程、关联 origin、push）。

之后你每次提交，那个按钮会变成 **`Sync Changes（同步更改）`**（带 `↑↓` 数字，告诉你有几个待推/待拉）。点它 = 先 pull 再 push，一键和云端对齐。

```
┌─ 顶部按钮的三种状态 ───────────────────────────┐
│  Publish Branch    → 第一次：把项目发布到 GitHub │
│  Sync Changes ↑2   → 有 2 个本地提交待推上去     │
│  Sync Changes ↓3   → 云端有 3 个提交待拉下来     │
└────────────────────────────────────────────────┘
```

---

## 🆚 加分关 · 解决合并冲突（VS Code 在这点上特别强）

🎯 *对应课程：模块 6——合并冲突*

万一合并时两边改了同一行，VS Code 不会让你对着 `<<<<<<<` 符号发懵，而是给你**彩色高亮 + 一排按钮**：

```
┌─ 冲突的文件里会长这样 ──────────────────────────┐
│  ┌ Accept Current │ Accept Incoming │ Both ┐    │ ← 一排可点的按钮
│  <<<<<<< 你的版本（Current，绿色高亮）           │
│      return "我会回来的"                         │
│  =======                                        │
│      return "后会有期"                           │
│  >>>>>>> 对方版本（Incoming，蓝色高亮）           │
└─────────────────────────────────────────────────┘
```

🖱️ 你只需点那排按钮里的一个：
- **Accept Current Change（采用当前更改）** = 留你这边的
- **Accept Incoming Change（采用传入的更改）** = 留对方的
- **Accept Both Changes（保留双方更改）** = 两个都留

点完，那些 `<<<`、`===`、`>>>` 符号会**自动消失**。再正常 Stage + Commit 一下，冲突就解决了。

💡 比手动删符号省心多了。记住课程那句：冲突不是错误，是 Git 在请你做决定。VS Code 只是把"做决定"变成了点按钮。

---

## 🌟 强烈推荐装两个扩展（让 Git 更好用）

🖱️ 点活动栏的 **扩展（⬛）** 图标，搜索安装：

| 扩展 | 作用 |
|------|------|
| **Git Graph** | 画出漂亮的"提交火车图"，整个历史和分支一目了然 |
| **GitLens** | 每行代码旁边显示"这行是谁、什么时候改的"，查历史神器 |

装完 Git Graph 后，源代码管理面板会多一个 **`View Git Graph`** 入口，点开就能看到课程里那种分支可视化图。

---

## 📌 速查：图形按钮 ↔ 命令 ↔ 课程概念

| 在 VS Code 里点… | 等于命令 | 课程概念 |
|---|---|---|
| Initialize Repository（初始化仓库） | `git init` | 建货舱 |
| 文件右边的 `+` | `git add` | 送上暂存区柜台 |
| ✓ Commit（提交） | `git commit` | 创建存档点 |
| 点文件名看左右对比 | `git diff` | 看穿改了啥 |
| 左下角分支名 → Create new branch | `git switch -c` | 开平行宇宙 |
| ⋯ → Branch → Merge Branch | `git merge` | 合并回主线 |
| 文件右边的 `↩` Discard | `git restore` | 丢弃没提交的改动 |
| Publish Branch / Sync Changes | `git push` / `git pull` | 和云端同步 |
| 冲突里的 Accept 按钮 | 手动解决冲突 | 拍板留哪个版本 |

---

## 🎓 命令行 vs 图形界面，用哪个？

💡 **没有对错，看场合：**
- **图形界面（VS Code）**：日常 90% 的活——看改了啥、提交、切分支、推送——点按钮又快又直观，还不会敲错。
- **命令行**：遇到复杂或罕见的操作，或者按教程/AI 的指示精确执行某条命令时。

✋ 高手两个都会，随手切换。但对你来说，**先把 VS Code 这套点熟，就足够应付绝大多数情况了。** 哪天 AI 让你"跑一条 git 命令"，你打开 VS Code 底部的终端（菜单 `Terminal → New Terminal`）粘进去就行——图形和命令行，在同一个窗口里随你用。

下一步想练什么、或哪一步点不到按钮，随时告诉我。
