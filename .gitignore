# ===== 让 Git 自动忽略这些文件 =====
# 写在这里的文件/文件夹，Git 会假装看不见——
# 它们不会出现在 git status 里，也永远不会被提交。

# macOS 系统自动生成的垃圾文件（在 Finder 里看不到，但真实存在）
.DS_Store

# 各种编辑器/系统的临时文件
Thumbs.db
*.log
*.tmp

# 如果以后这个项目用了 npm，依赖文件夹通常体积巨大、不该提交，也忽略掉：
node_modules/
